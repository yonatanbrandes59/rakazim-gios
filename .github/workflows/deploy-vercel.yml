name: Deploy to Vercel

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment triggered
        run: |
          echo "✅ Push to master — Vercel auto-deploy triggered via GitHub integration"
          echo "📋 Survey: https://rakazim-gios.vercel.app/apply"
          echo "🔧 Admin:  https://rakazim-gios.vercel.app/admin"
